name: localpkg
description: "A new, object-oriented localpkg Flutter version."
version: 0.0.0
homepage: https://github.com/Calebh101/localpkg-flutter-2
publish_to: none

environment:
  sdk: ^3.8.1
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
